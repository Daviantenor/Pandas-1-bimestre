plt.figure(figsize=(5, 5))
sns.boxplot(data=df, x="categoria", y="faturamento")
plt.title("faturamento por categoria")
plt.xlabel("categoria")
plt.ylabel("faturamento")
plt.show()
print(df[df["faturamento"]== df["faturamento"].max()])



Exercicio 4 2.0


plt.figure(figsize=(5, 5))
sns.boxplot(data=df, x="categoria", y="faturamento",hue="categoria")
plt.title("faturamento por categoria")
plt.xlabel("categoria")
plt.ylabel("faturamento")
plt.show()
print(df[df["faturamento"]== df["faturamento"].max()])
